import { registerRootComponent } from "expo";

import App from "./App";

// Expo entry point: registers the root component for both Expo Go and the
// standalone (prebuilt) native app.
registerRootComponent(App);
